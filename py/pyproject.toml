[project]
name = "stock-backtester"
version = "0.1.0"
description = "Stock trading backtester with DMA pullback strategy"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
backtester = "backtester.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["src/backtester"]
